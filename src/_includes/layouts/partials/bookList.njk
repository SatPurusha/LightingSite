{% for image in lightingBook %}  
  <h2 id="{{ image.title }}">{{ image.title }}</h2>
  <p>{{ image.text }}</p>
  <ul>
    <li class="projects">{{ image.projects }}</li>
  </ul>
  <div class="book-image">
    <img class = "aspect" src="{{ image.link }}.jpg" alt="{{ image.alt }}"/>
  </div>
{% endfor %}